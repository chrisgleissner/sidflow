# SIDFlow Production Environment Configuration
# Copy this file to .env and configure your settings

# Required: Admin credentials for the web admin interface
# Use a strong password (minimum 16 characters recommended)
SIDFLOW_ADMIN_USER=admin
SIDFLOW_ADMIN_PASSWORD=your-secure-password-here

# Optional: Secret key for secure cookie signing (generates if not set)
# SIDFLOW_ADMIN_SECRET=your-secret-key-here

# Optional: Path to your SID collection (HVSC or custom)
# Used by deployment scripts/templates when generating config and volumes.
# SIDFLOW_SID_PATH=/path/to/your/hvsc

# Optional: Path for WAV cache (rendered audio files)
# SIDFLOW_AUDIO_CACHE=/path/to/audio-cache

# Optional: Path for user ratings/tags data
# SIDFLOW_TAGS_PATH=/path/to/tags

# Optional: Path for application data (LanceDB, classifications, preferences, etc.)
# SIDFLOW_DATA_PATH=/path/to/sidflow-data

# Optional: Ultimate 64 hardware password (if using hardware SID playback)
# ULTIMATE64_PASSWORD=your-ultimate64-password
