[test]
# Exclude E2E tests from bun test (they run via playwright test)
exclude = [
  "packages/sidflow-web/tests/e2e/**/*",
  "**/*.e2e.ts",
  "**/*.spec.ts",
  "**/tests/e2e/**"
]

# Increase default timeout for tests that run TensorFlow or WASM
timeout = 120000
