# Archived plans (2025-12 to 2026-03)

Archived from `PLANS.md` on 2026-03-13 to restore a single convergent active roadmap in the repository root plan. These entries preserve user intent, disposition, and progress highlights without keeping the active planning surface overloaded.

## Completed tasks

### Production-readiness and portability audit (2026-03-13)

Status: completed

Request summary:

- Perform an exhaustive, evidence-based production-readiness and feature-completeness audit of SIDFlow.
- Cover Fly.io rollout readiness, 100-concurrent-user viability, and a portable SID correlation export feature.

Progress highlights:

- Reviewed required repo docs and production-critical code paths.
- Verified rollout risks in auth, persistence, jobs, API contracts, observability, and performance tooling.
- Inspected sibling `c64commander` to ground the export design in HVSC-relative path identity and mobile use.
- Wrote `doc/audits/audit1/audit.md`.

Validation evidence:

- `bun run test` x3 consecutive:
  - 1666 pass, 0 fail, 6047 expect() calls. Ran 1666 tests across 165 files. [122.12s]
  - 1666 pass, 0 fail, 6047 expect() calls. Ran 1666 tests across 165 files. [116.27s]
  - 1666 pass, 0 fail, 6047 expect() calls. Ran 1666 tests across 165 files. [115.24s]

### Fix build/test SIGHUP and TS errors (2025-12-21)

Status: completed

Request summary:

- Stop `bun run test` from crashing and clear blocking TypeScript errors so the build/test pipeline is green.

Progress highlights:

- Reduced render integration test resource pressure.
- Stabilized libsidplayfp realtime playback stress behavior under constrained threads.
- Fixed `generateAutoTags` scoping issues.

Validation evidence:

- `bun run test` x3 consecutive:
  - 1666 pass, 0 fail, 6047 expect() calls. Ran 1666 tests across 165 files. [87.05s]
  - 1666 pass, 0 fail, 6047 expect() calls. Ran 1666 tests across 165 files. [85.80s]
  - 1666 pass, 0 fail, 6047 expect() calls. Ran 1666 tests across 165 files. [86.48s]

### Iterative station optimization (richer Essentia features + extreme seeds) (2025-12-19)

Status: completed

Request summary:

- Improve station coherence with richer Essentia features, extreme seeds, and deterministic rating/verification logic.

Progress highlights:

- Expanded feature extraction with MFCC and spectral descriptors.
- Introduced deterministic dataset-normalized feature-to-rating mapping.
- Reclassified DEMOS/G-L, rebuilt stations, and added WAV similarity verification tooling.

Validation evidence:

- `bun run test` x3 consecutive:
  - 1663 pass, 0 fail, 6034 expect() calls. Ran 1663 tests across 164 files. [86.83s]
  - 1663 pass, 0 fail, 6034 expect() calls. Ran 1663 tests across 164 files. [87.60s]
  - 1663 pass, 0 fail, 6034 expect() calls. Ran 1663 tests across 164 files. [86.85s]

### Run web on DEMOS subset + classify via admin (2025-12-19)

Status: completed

Request summary:

- Run the web UI locally against a DEMOS subset, trigger classification via the web API, and verify progress/admin visibility.

Progress highlights:

- Fixed subset root/path resolution for the web app.
- Confirmed classification progress reporting and artifact generation under sandbox paths.
- Built proof station folders from the produced classification data.

Validation evidence:

- `bun run build` OK.
- `bun run test` x3 consecutive:
  - 1661 pass, 0 fail, 6028 expect() calls. Ran 1661 tests across 163 files. [73.61s]
  - 1661 pass, 0 fail, 6028 expect() calls. Ran 1661 tests across 163 files. [75.67s]
  - 1661 pass, 0 fail, 6028 expect() calls. Ran 1661 tests across 163 files. [75.93s]

### Improve station coherence (BPM estimator + seeded verification) (2025-12-19)

Status: completed

Request summary:

- Investigate incoherent stations and specifically verify the `Instantfunk.sid` / `Kaori_360.sid` case.

Progress highlights:

- Replaced placeholder BPM behavior with autocorrelation-based estimation.
- Reclassified DEMOS/G-L and rebuilt stations with confidence-aware BPM weighting.
- Added deterministic `--seed-key` station inspection and verified the target bad pairing was not present.

Validation evidence:

- `bun run build` OK.
- `bun run test` OK (1663 pass, 0 fail).

### Fix nightly k6 perf flake on /api/play (2025-12-15)

Status: completed

Request summary:

- Fix intermittent nightly k6 failures caused by transient `POST /api/play` transport errors.

Progress highlights:

- Hardened generated k6 scripts with retries, defensive JSON parsing, and VU jitter.

Validation evidence:

- `npm run test` x3 consecutive OK (1661 pass, 0 fail).

### Documentation accuracy + link integrity sweep (2025-12-14)

Status: completed

Request summary:

- Review docs, comments, and UI descriptions for accuracy, concision, and link integrity.

Progress highlights:

- Fixed broken doc references.
- Updated README, technical reference, OpenAPI, UI copy, and CLI help text to better match implementation.

Validation evidence:

- `npm run build` OK.
- `npm run test` x3 consecutive OK.

### Configurable intro skip + updated render/classify constraints (2025-12-13)

Status: completed

Request summary:

- Make intro skip configurable and enforce sensible render/classify timing constraints in config, API, and UI.

Progress highlights:

- Added `introSkipSec` to config and prefs surfaces.
- Updated representative-window selection logic.
- Tightened render/classify constraint validation.

Validation evidence:

- `bun run build` OK.
- `bun run test` x3 consecutive:
  - 1661 pass, 0 fail, 6028 expect() calls. Ran 1661 tests across 163 files. [83.34s]
  - 1661 pass, 0 fail, 6028 expect() calls. Ran 1661 tests across 163 files. [77.16s]
  - 1661 pass, 0 fail, 6028 expect() calls. Ran 1661 tests across 163 files. [74.87s]

### Respect format prefs; trim WAV intro/silence; enforce render>=classify (2025-12-13)

Status: completed

Request summary:

- Respect format preferences, remove leading silence from rendered WAVs, postprocess representative snippets, and enforce render/classify bounds.

Progress highlights:

- Wired classify temp config to honor `defaultFormats`.
- Added WAV trimming/representative-window postprocessing.
- Tightened UI/API validation.

Validation evidence:

- `bun run test` x3 consecutive:
  - 1656 pass, 2 skip, 0 fail. Ran 1658 tests across 163 files. [50.61s]
  - 1656 pass, 2 skip, 0 fail. Ran 1658 tests across 163 files. [50.33s]
  - 1656 pass, 2 skip, 0 fail. Ran 1658 tests across 163 files. [50.72s]

### Preserve cached WAVs; cap feature extraction window (2025-12-13)

Status: completed

Request summary:

- Preserve oversized cached WAVs on cache hits and cap analysis to a representative fragment while limiting render-time creation only when new artifacts are built.

Progress highlights:

- Removed cache-hit truncation.
- Added representative-window extraction logic for feature extraction.
- Kept render-time limiting only for new artifacts.

Validation evidence:

- `bun run build` x3 passed.
- `bun run test` x3 consecutive: 1650 pass, 2 skip, 0 fail.

### Wire maxRenderSec/maxClassifySec into prefs UI/API (2025-12-13)

Status: completed

Request summary:

- Expose `maxRenderSec` and `maxClassifySec` through prefs UI/API with fast tests.

Progress highlights:

- Wired config limits through `/api/prefs` and the admin prefs UI.
- Added fast API route and classification edge-case tests.

Validation evidence:

- `bun run build && bun run test` x3: 1643 pass, 2 skip, 0 fail.

## Superseded or absorbed tasks

### WAV playlist manifests (2025-12-20)

Status: superseded by delivered directory-playlist/station manifest work and by the new convergence roadmap

Reason archived:

- The original intent was effectively absorbed by later directory playlist/station output work.
- Remaining questions are better handled under the active roadmap, not as an isolated legacy task.

Preserved progress:

- Created plan and audited WAV emitters on 2025-12-20.

### Classification speed vs station deviation journey (DEMOS/G-L) (2025-12-19)

Status: superseded and partially absorbed into the active roadmap

Reason archived:

- The task mixed station quality work, performance experimentation, and validation hardening.
- Most enabling work landed, but the remaining convergence belongs under the new roadmap’s scale-validation and launch-readiness phases.

Preserved progress highlights:

- Added deterministic station comparison, config-path fixes, render determinism fixes, cache invalidation, tempo constraints, and journey-runner hardening.
- Re-verified baseline determinism at 1.0000 mean/min Jaccard.
- Last open state focused on broader validation and final journey selection.

### Fix WASM ROM setup for BASIC/RSID tunes (2025-12-19)

Status: mostly completed; remaining artifact regeneration absorbed into later classification/station work

Reason archived:

- ROM injection was implemented and verified.
- The remaining “re-render the affected subset” step was effectively folded into later reclassification/station rebuild tasks.

Preserved progress highlights:

- Implemented KERNAL/BASIC/CHARGEN ROM loading for the WASM renderer.
- Verified healthy audio output on `_BASIC.sid` samples.

### Production readiness review (core pipeline + web) (2025-12-14)

Status: superseded by the newer, evidence-based production audit and active roadmap

Reason archived:

- This broad review task was an earlier precursor to the explicit audit in `doc/audits/audit1/audit.md`.
- Remaining productionization work is now captured more clearly in the convergent roadmap.

Preserved progress highlights:

- Fixed `validate:config` dependency breakage, hardened `/api/charts`, aligned standalone server start behavior, stabilized sample scripts, and made WASM perf assertions opt-in.
- Verified build, config validation, unit tests, e2e tests, and `ci:verify`.

### Performance test reliability + regression detection (2025-12-14)

Status: partially completed; remaining scale-proof work absorbed into the active roadmap

Reason archived:

- The reliability/tooling work largely landed.
- The unresolved part is not runner correctness anymore; it is proving the production workload and topology, which now belongs in the dedicated scale-validation phase.

Preserved progress highlights:

- Added runner profiles, k6 SLO checks, improved `/api/play` parsing, CI perf smoke, and standalone binding fixes.

### Enable all skipped tests + fix classify-heartbeat idle timeout (2025-12-13)

Status: superseded by later classify/test stability work

Reason archived:

- Later tasks substantially reworked classify stability and test reliability.
- The active roadmap now owns broad validation gates rather than preserving this older narrow task as a standalone item.

Preserved progress:

- Inventory started; initial heartbeat failure/root-cause work was recorded on 2025-12-13.

### Fix classify-heartbeat e2e test (2025-12-12)

Status: superseded by later classify progress/stability work

Reason archived:

- The targeted Playwright conversion work was useful, but the remaining task is better handled as part of current validation and job-state hardening.

Preserved progress:

- Converted the heartbeat spec to Playwright and added idle/stale tracking via the API.

### Fix Fly.io CI deploy (2025-12-12)

Status: partially completed; remaining rollout work absorbed into the active roadmap

Reason archived:

- Some workflow and image compatibility fixes landed.
- The unresolved deployment problem requires broader topology/secret/state decisions that are now explicitly handled in the rollout roadmap.

Preserved progress highlights:

- Updated workflow defaults for app/volume creation.
- Added Docker compatibility symlinks.
- Confirmed local validation was blocked by Fly token/app-creation scope.

## Older legacy archive summary already present in historical PLANS

The previous `PLANS.md` also carried a compact “Archived Tasks” summary for earlier December 2025 work, including:

- Classification Pipeline Hardening & Productionization (2025-12-06)
- CI Build Speed & Test Stability (2025-12-06)
- Classification Pipeline Fixes (2025-12-04)
- Codebase Deduplication & Cleanup (2025-12-04)
- Documentation Consolidation Phase 1 & 2 (2025-12-06)

Those summaries remain recoverable in git history and did not need to stay in the active planning surface after the 2026-03 convergence cleanup.
