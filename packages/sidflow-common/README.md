# @sidflow/common

Shared utilities: config loader, logging, retry, filesystem helpers.

## Key Exports

- `loadConfig()` — Load `.sidflow.json`
- `stringifyDeterministic()` — Stable JSON serialization
- `createLogger()` — Structured logging
