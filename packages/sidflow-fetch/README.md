# @sidflow/fetch

HVSC collection download and sync.

## Usage

```bash
bun ./scripts/sidflow-fetch [--config <path>] [--remote <url>] [--version-file <path>]
```

Downloads to `sidPath` configured in `.sidflow.json`.
