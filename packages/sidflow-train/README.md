# @sidflow/train

TensorFlow.js model training from user feedback.

## Usage

```bash
bun ./scripts/sidflow-train [--config <path>] [--epochs N] [--batch-size N] [--learning-rate N] [--evaluate|--no-evaluate] [--force]
```

Output: `data/model/` (model.json + weights)
