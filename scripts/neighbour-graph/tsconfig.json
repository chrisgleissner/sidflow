{
  "//": "Type-checks the neighbour-graph measurement scripts, which the root tsconfig does not cover (it only references packages/*). Bun strips types without checking them, so without this the scripts are unchecked and a renamed field fails silently at runtime. Strictness is inherited from tsconfig.base.json and deliberately not raised: these scripts import package sources directly, so extra flags here would apply to the whole imported graph.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false
  },
  "include": ["**/*.ts"]
}
